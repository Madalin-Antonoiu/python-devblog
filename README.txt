1. After you clone the repo, create a venv at the root folder next to main
2. Activate it and run server.


3. Get into venv:
cd venv/Scripts/ && . activate && cd .. && cd .. && cd main  
py manage.py runserver

The static folder should be inside main/blog ( inside your app directory)